import { DocsLayout } from "@/components/docsLayout";

export const meta = {
	title: "CHITUBOX Pro FAQ",
	author: "CHITUBOX Team",
};

# CHITUBOX Pro FAQ

<Admonition type="tip">
	You can use **CTRL + F** to search content on this page.
</Admonition>

## Windows Issues

<Admonition type="note">
	Please make sure you've installed all <NextLink href="/en-US/docs/chitubox-pro/latest/requirements">required environments</NextLink>.
</Admonition>

### MSVCR110.dll Missing

"MSVCR110.dll Missing" means you're missing Visual C++ Redistributable Packages for Visual Studio 2012, please download the required environments from <SpanA href="https://www.microsoft.com/en-us/download/details.aspx?id=30679">here</SpanA>.

### MSVCR120.dll Missing

"MSVCR120.dll Missing" means you're missing Visual C++ Redistributable Packages for Visual Studio 2013, please download the required environments from <SpanA href="https://www.microsoft.com/en-us/download/details.aspx?id=40784">here</SpanA>.

### MSVCR140.dll Missing

"MSVCR140.dll Missing" means you're missing Visual C++ Redistributable Packages for Visual Studio 2015, 2017 and 2019, please download the required environments from <SpanA href="https://docs.microsoft.com/en-us/cpp/windows/latest-supported-vc-redist?view=msvc-170">here</SpanA>.

### Other System-Level Errors

-   Error -1073741701

-   Error -1073741515

-   Error -1073740791

-   Error -0xc000007b

These errors might occur due to some corrupted or damaged operating system files.

Please try the following methods:

Method 1:
Open cmd on your PC (By using Win + R and input cmd), enter following command:

```shell
DISM.exe /Online /Cleanup-image /Scanhealth

DISM.exe /Online /Cleanup-image /Restorehealth
```

Method 2:
Open cmd on your PC (by using Win + R and input cmd), enter following command:

```shell
sfc /scannow
```

If these method doesn't solve these errors, you might need to download and reinstall your system from Microsoft's official website.

-   <SpanA href="https://www.microsoft.com/en-us/software-download/windows11">Windows 11</SpanA>

-   <SpanA href="https://www.microsoft.com/en-us/software-download/windows10">Windows 10</SpanA>

### Windows Explorer Keep Restart Repeatedly

If your Windows Explorer keep reastart repeatedly, please follow these steps to fix it:

Invoke your Task Manager by pressing Ctrl + Shift + Esc, right-click Windows Explorer, then click End Task (not Restart).

<StyledAntdImage src={"/images/docs/en-US/faq/chitubox-pro/1.x.x/faq002.png"} />

In your Task Manager, click File > Run new task, input `cmd`, check **Create this task with administrative privileges**, then click **OK**.

Input:

```shell
regsvr32 /u "{CHITUBOX-Installation-Path}\CHITUBOX_Thumbnail.dll"
```

Note you need to replace `{CHITUBOX-Installation-Path}` with your CHITUBOX's installation path, for example:

```shell
regsvr32 /u "C:\Program Files\CHITUBOXPro V1.0.1\CHITUBOX_Thumbnail.dll"
```

Hit Enter to run it.

<Admonition type="note">
	Your OpenGL version should not older than 2.0.
</Admonition>

### Conflict with Fusion 360 or Failed to create OpenGL context for format QSurfaceFormat

<StyledAntdImage src={"/images/docs/en-US/faq/chitubox-pro/1.x.x/faq001.png"} />

Please open System Settings> About> Advanced System Settings and remove the environment variable `QT_OPENGL`.

### This application failed to start because no Qt platform plugin could be initialized

<StyledAntdImage src={"/images/docs/en-US/faq/chitubox-pro/1.x.x/faq006.png"} />

Please open System Settings> About> Advanced System Settings and remove the two environment variables `QT_OPENGL` and `QT_PLUGIN_PATH`.

### CHITUBOX Pro is Not Responding

<StyledAntdImage src={"/images/docs/en-US/faq/chitubox-pro/1.x.x/faq008.png"} />

Most likely it is a software conflict issue, please quit your runtime software one by one to track down which software conflicts with CHITUBOX. After this is done, the problem should be solved for most cases.
The following software may cause the conflict:

-   Anti-virus software

-   Tabtip (In Windows Task Manager it is called Touch Keyboard and Handwriting Panel)

-   PowerToys

-   Google Drive

## Plugin Issues

### Longer Error -1

1. Longer's control board process the resolution specially, we have confirmed the correct resolution x: 960, y: 1708. We have double-check and confirmed this, so please don't change it.

2. `Error code -1` After changing the resolution is an error in the internal code of the plug-in. Because you changed the resolution, an error was reported.

Download Longer plugin <SpanA href="https://drive.google.com/drive/u/0/folders/1rPRw68b3Z1oqLwc7hSmP5A9Zx0HjatbE?fbclid=IwAR2XyvSs_jCWwqXymOh9ZEvcuyKNuj-ibnmbLAmfOsZMWzjJdke71kxANwQ">here</SpanA>.

## File Issues

### Thumbnail Abnormal

<StyledAntdImage src={"/images/docs/en-US/faq/chitubox-pro/1.x.x/faq004.png"} />

Hold Ctrl + Shift + Esc to open the Task Manager, click File > Run new task, input `cmd`, check **Create this task with administrative privileges**, then click **OK**.

Input:

```shell
regsvr32 /u "{CHITUBOX-Installation-Path}\CHITUBOX_Thumbnail.dll"
```

Note you need to replace `{CHITUBOX-Installation-Path}` with your CHITUBOX's installation path, for example:

```shell
regsvr32 /u "C:\Program Files\CHITUBOXPro V1.0.1\CHITUBOX_Thumbnail.dll"
```

Hit Enter to run it.

<StyledAntdImage
	src={"/images/docs/en-US/faq/chitubox-pro/1.x.x/faq004-1.png"}
/>

Input:

```shell
regsvr32 "{CHITUBOX-Installation-Path}\CHITUBOX_Thumbnail.dll"
```

Hit Enter to run it.

<StyledAntdImage
	src={"/images/docs/en-US/faq/chitubox-pro/1.x.x/faq004-2.png"}
/>

**Disk Cleanup**<br />

Find your system disk (usually C drive), right-click to select properties, and click "Disk Cleanup".

<StyledAntdImage
	src={"/images/docs/en-US/faq/chitubox-pro/1.x.x/faq004-3.png"}
/>

Check "Thumbnails", then click "Clean up system files", and then click "OK".

<StyledAntdImage
	src={"/images/docs/en-US/faq/chitubox-pro/1.x.x/faq004-4.png"}
/>

Select "Delete Files".

<StyledAntdImage
	src={"/images/docs/en-US/faq/chitubox-pro/1.x.x/faq004-5.png"}
/>

**Restart Explorer**

Invoke your Task Manager by pressing `Ctrl + Shift + Esc` , right-click Windows Explorer, then click End Task (not Restart).

### Surface of Models Go Rough after Slicing

After slicing the model, you may think that the quality of the image is rough, but in fact, this is the model shows every voxel through stereo rendering after sliced. As if you can see the appearance of each pixel after you zoom on image in Photoshop.

As shown in the figure, the height of each cube is the layer height set in the slice setting. The length is the length of each pixel in the horizontal direction on the screen. The width is the width of each pixel in the vertical direction on the screen. Length, width, and height constitute the volume of the voxel. A voxel is the smallest unit that constitutes a print.

<StyledAntdImage
	src={"/images/docs/en-US/faq/chitubox-pro/1.x.x/faq005.png"}
	style={{ maxwidth: "400px" }}
/>

<Admonition type="note">
	This voxel image cannot be seen during the preview after the slicing. You
	need to reload the .ctb file in CHITUBOX to see it.
</Admonition>

### Why STL files were scaled after importing to CHITUBOX?

An STL file doesn't contain the exact unit, it only contains numbers. Therefore, different software read an STL in different units. For example, when using STL file, 1m exported from Blender will be read as 1mm in CHITUBOX. You need to test the corresponding proportion of your 3D modeling software and CHITUBOX before you export your models.

## Other Issues

For some reason, you might encounter other issues caused by data corruption between versions. You can try the following ways to solve issues:

### Clean the Cache

1. Open the log path (Setting/Help > Open Log). <NextLink href="/en-US/docs/chitubox-pro/latest/application-overview/setting-and-help#open-log">📃Click here</NextLink> to learn how to open the log.

1. Close CHITUBOX Pro (**the order matters**).

1. Make sure you already closed CHITUBOX Pro, back up the 📂*machine* folder to another location, then delete all files and folders except 📄*userLogin.cfg* file.

	<StyledAntdImage
		src={"/images/docs/en-US/faq/chitubox-pro/1.x.x/faq003.png"}
	/>

1. Restart CHITUBOX Pro.

1. Optional, after restarting CHITUBOX Pro, a new 📂*machine* folder will be generated, you can restore your machines by copying and pasting _.cfg_ files back to the new 📂*machine* folder. Please do not copy all _.cfg_ files back to the 📂*machine* folder at once given the bug you encountered might be caused by one of them.

## USB Drive Issues

You might found that your printer can't read your USB drive properly sometimes. Mostly this is because your printer's control board was designed to work with USB 2.0 and FAT32 format with a volume equals to or lower than 16GB. USB 3.0 uses a larger current therefore can cause electrical interference to the control board sometimes. In summary, your USB drive should meet following ments:

-   FAT32 format

-   USB 2.0

-   ≤ 16GB

-   Allocation unit size = 2048 bytes

	<StyledAntdImage src={"/images/docs/en-US/faq/chitubox-pro/1.x.x/faq009.png"} />

## How to Join Our Affiliate

1. Log in your account on www.chitubox.com.

	<StyledAntdImage
		src={"/images/docs/en-US/faq/chitubox-pro/1.x.x/faq007-1.png"}
		style={{ maxwidth: "700px" }}
	/>

1. Edit your channel information and apply to be an affiliate.

	<StyledAntdImage
		src={"/images/docs/en-US/faq/chitubox-pro/1.x.x/faq007-2.png"}
		style={{ maxwidth: "700px" }}
	/>

1. You can check more details on your affiliate center when we approval.

	<StyledAntdImage
		src={"/images/docs/en-US/faq/chitubox-pro/1.x.x/faq007-3.png"}
		style={{ maxwidth: "700px" }}
	/>

1. You can input the landing page link here to generate your customized link.

	<ImageContainer>
		<StyledAntdImage
			src={"/images/docs/en-US/faq/chitubox-pro/1.x.x/faq007-4.png"}
			style={{ maxwidth: "700px" }}
		/>
	</ImageContainer>

	<ImageContainer>
		<StyledAntdImage
			src={"/images/docs/en-US/faq/chitubox-pro/1.x.x/faq007-5.png"}
			style={{ maxwidth: "700px" }}
		/>
	</ImageContainer>

export default ({ children }) => (
	<DocsLayout meta={meta}>{children}</DocsLayout>
);
