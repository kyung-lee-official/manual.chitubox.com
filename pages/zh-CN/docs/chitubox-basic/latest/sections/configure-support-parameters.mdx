import { DocsLayout } from "@/components/docsLayout";

export const meta = {
	title: "Configure Support Parameters",
	author: "CHITUBOX Team",
	pageId: "/zh-CN/chitubox-basic/v2.0.0/configure-support-parameters"
};

# Configure Support Parameters

export default ({ children }) => (
	<DocsLayout meta={meta}>{children}</DocsLayout>
);
