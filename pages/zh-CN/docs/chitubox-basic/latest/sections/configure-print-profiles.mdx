import { DocsLayout } from "@/components/docsLayout";

export const meta = {
	title: "Configure Print Profiles",
	author: "CHITUBOX Team",
	pageId: "/zh-CN/chitubox-basic/v2.0.0/configure-print-profiles"
};

# Configure Print Profiles

export default ({ children }) => (
	<DocsLayout meta={meta}>{children}</DocsLayout>
);
