import { DocsLayout } from "@/components/docsLayout/DocsLayout";
import { ChituboxAddMachine } from "@/components/icons/Icons";

export const meta = {
	title: "设置打印参数",
	author: "CHITUBOX Team",
	pageId: "/zh-CN/chitubox-basic/v2.0.0/configure-print-profiles",
};

# 设置打印参数

要配置打印参数，你需要先添加一台打印机，如果你还没添加打印机，点击主窗口下方的添加机器按钮 <ChituboxAddMachine /> 添加一台打印机。

一台打印机可以添加多个切片配置文件，你可以在**切片设置列表**管理你的切片配置文件，你可以在这里下载，上传，添加，删除，导入，导出，以及重置你的切片配置文件。

<ImageContainer>
	<AntdImage
		src={
			"/images/docs/zh-CN/chitubox-basic/2.x.x/009-setting-up-configure-printer-parameters001.png"
		}
	/>
</ImageContainer>

单击主窗口右下方的 “切片设置” 按钮打开切片配置文件窗口。

## 切片设置

### 机器

| 参数         | 说明                                                                             |
| ------------ | -------------------------------------------------------------------------------- |
| 名称         | 当前打印机的名称                                                                 |
| 机器类型     | 机器的类型说明                                                                   |
| 镜像         | 镜像模式选择                                                                     |
| 分辨率       | X: X 轴方向的像素个数 <br/> Y: Y 轴方向的像素个数                                |
| 尺寸         | X 轴方向的最大打印尺寸 <br/> Y 轴方向的最大打印尺寸 <br/> Z 轴方向的最大打印尺寸 |
| 锁定比例     | X 轴和 Y 轴的尺寸和分辨率的比值相等                                              |
| 构建区域偏移 | 设定打印区域边界线                                                               |

### 树脂
### 打印
### GCode
### 高级

export default ({ children }) => (
	<DocsLayout meta={meta}>{children}</DocsLayout>
);
