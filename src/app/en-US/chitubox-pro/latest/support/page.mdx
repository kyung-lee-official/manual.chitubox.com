import { Entry } from "@/docComponents/entry/Entry";

export const metadata = {
	title: "Support",
	authors: [{ name: "CHITUBOX Team" }],
	description: "This chapter introcues the support functions in CHITUBOX Pro.",
	other: {
		pageId: "/en-US/chitubox-pro/v2.0.0/support",
	},
	openGraph: {
		images: ["/images/docs/og_logo.png"],
	},
};

# Support

Deep dive into the support functions in CHITUBOX Pro to create reliable supports for your models.

<Entry functionMenu={["Support"]} />

Click the Support tab on the function menu to go into the support window.
