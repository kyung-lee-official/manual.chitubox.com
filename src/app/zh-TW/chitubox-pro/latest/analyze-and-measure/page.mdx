import {
	DisplaySize,
	TwoPointDistance,
	PointToPlaneDistance,
	ParallelPlaneDistance,
} from "@/components/icons/ChituboxProIcons";
import { ChituboxRulerIcon } from "@/components/icons/Icons";
import { Entry } from "@/docComponents/entry/Entry";

export const metadata = {
	title: "分析/測量",
	authors: [{ name: "CHITUBOX Team" }],
	description: "CHITUBOX Pro 中的分析/測量界面。",
	other: {
		pageId: "/zh-TW/chitubox-pro/v2.0.0/analyze-and-measure",
	},
	openGraph: {
		images: ["/images/docs/og_logo.png"],
	},
};

# 分析/測量

## 顯示大小

<Entry
	functionMenu={[
		"分析/測量",
		<div className="flex items-center gap-1">
			<DisplaySize />
			顯示大小
		</div>,
	]}
	componentList={[
		<div className="flex items-center gap-1">
			<ChituboxRulerIcon /> 
		</div>,
	]}
/>

<DisplaySize /> 自動測量並顯示選定模型的<Link href={"https://zh.wikipedia.org/wiki/%E5%8C%85%E5%9B%B4%E4%BD%93"}>包圍體</Link>尺寸。

## 兩點距離

<TwoPointDistance /> 測量並顯示模型兩點之間的距離值。

## 點到平面距離

<PointToPlaneDistance /> 測量並顯示模型上點到平面的距離值。

## 平行平面距離

<ParallelPlaneDistance /> 測量並顯示模型上兩平面的距離值。
