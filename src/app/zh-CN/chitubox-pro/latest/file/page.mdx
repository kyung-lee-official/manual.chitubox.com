import { Entry } from "@/docComponents/entry/Entry";
import { ChituboxOpenIcon, ChituboxSaveIcon } from "@/components/icons/Icons";
import { HistoryIcon, ExportIcon } from "@/components/icons/ChituboxProIcons";

export const metadata = {
	title: "文件",
	authors: [{ name: "CHITUBOX Team" }],
	description: "本节",
	other: {
		pageId: "/zh-CN/chitubox-pro/v2.0.0/printer-management",
	},
	openGraph: {
		images: ["/images/docs/og_logo.png"],
	},
};

# 文件

## 项目文件

### 打开和保存项目文件

<ChituboxOpenIcon /> 打开项目文件、模型文件或切片文件。

<ChituboxSaveIcon /> 保存为项目文件或模型文件。

### 文件命名

### 兼容性

### 临时文件

## 3D 网格文件

### 支持的 3D 网格文件格式

### 导入和导出 3D 网格文件

<ExportIcon /> 导出文件，共有全部导出、合并导出、分部导出可选。

### 打开最近的文件

<HistoryIcon /> 打开在软件中加载过的项目文件、模型文件或切片文件。

## 切片文件

### 支持的切片文件格式

### 导入和导出切片文件

### 使用插件导出切片文件

## 配置文件
